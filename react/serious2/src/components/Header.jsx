import React from "react";

export default function Header() {
    const headerStyle = {
        backgroundColor: "blue",
        color: "white"
    };
    return (
        <header>
            <h1>Gloceries List</h1>
        </header>
    );
}